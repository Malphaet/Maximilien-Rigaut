program sialors;

var a, b : integer; c,d: array [1..3] of integer;

begin
{
   f(3,2);
}
   f(A>2);
end.

   if a = 0 then
      write (2);
   else
   begin
      b := a + 2;
      write(b);
   end;
end.
